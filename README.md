A simple banking program

This program took me a little bit of time and had a few errors which I had to fix but i managed to learn how to better implement methods.

Notes:

Don't close scanners inside of methods.
Use methods to update variables then print those updated variables instead of just having the method return a value to print.
Use methods to not repeat ourselves(DRY) or just to organise better.
